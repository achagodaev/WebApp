@model IEnumerable<WebApp.UI.ViewModels.SizeViewModels.SizeViewModel>
@using WebApp.UI.Controllers

@{
    ViewBag.Title = "Размеры";
}

<div class="row">
    <div class="col-md-12">
        <h4>Размеры</h4>
        <hr />

        <ol class="breadcrumb">
            <li class="active">Размеры</li>
        </ol>

        <p>
            @Html.ActionLink("Создать", nameof(SizesController.Create))
        </p>

        <table class="table table-condensed table-bordered">
            @if (Model.Any())
            {
                <tr class="active">
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th></th>
                </tr>

                foreach (var size in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => size.Name)
                        </td>
                        <td>
                            @Html.ActionLink("Подробная информация",
                                nameof(SizesController.Details),
                                new { id = size.Id }) |
                            @Html.ActionLink("Удалить",
                                nameof(SizesController.Delete),
                                new { id = size.Id })
                        </td>
                    </tr>
                }

                <tr class="active">
                    <th colspan="2">
                        Всего: @Model.Count()
                    </th>
                </tr>
            }
            else
            {
                <tr class="active">
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                </tr>
                <tr class="active">
                    <td>Нет размеров</td>
                </tr>
            }
        </table>
    </div>
</div>